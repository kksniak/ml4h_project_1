# -1 if all GPUs should be used, 0 if no GPUs should be used
USE_GPU = -1

# True if models should be retrained from scratch, False if they
# should be loaded from file
RETRAIN_MODELS = False

# True if results should not be saved, False otherwise
DRY_RUN = False
